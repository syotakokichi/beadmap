# devlog: 2026-08-16 — 境界ゲートの記事化と README の記事リンク更新

日付: 2026-08-16

## 何をしたか

境界ゲートの設計（fail-closed の 3 条件・検査する 3 範囲・語を足すと全履歴を検査し直す構造）と、
公開直前に個人メールアドレスの混入を回収した経緯（Day 2 の記録）を記事にした。

このリポジトリ側の変更は 2 点だけで、コードは変更していない。

- README「開発の歩み」の 2026-07-20（Day 2）行の記事リンクを、その日の内容に対応する新記事へ向け直した
- この devlog を追加し、歩み表に 2026-08-16 の行を足した

記事: <https://zenn.dev/syommy_program/articles/beadmap-boundary-gate>

## 訂正: Day 2 の記述が実装より先走っていた

記事化にあたって当時のコミットを読み直したところ、[Day 2 の devlog](2026-07-20-day2.md) の
「境界ゲートを git 履歴まで拡張 — 検査対象を作業ツリーだけでなく全コミット履歴
（blob 内容・author/committer）に広げた」は、**その時点の実装より広く書いていた**ことが分かった。

| コミット | 日時 | 検査範囲 |
| --- | --- | --- |
| `9281b36` 検証レビュー指摘の反映 | 2026-07-19 21:55 | 作業ツリー + 全コミット履歴の blob（**メタデータ検査なし**） |
| `74b2c02` beads DB 再構築・個人メール排除 | 2026-07-19 22:18 | （同上） |
| `9750c7e` 事後マルチレビュー指摘の反映 | 2026-07-20 22:50 | 上記 + **コミットメタデータ**（`git log --all` を grep） |

つまり実戦で NG を返したのは作業ツリーと履歴 blob の 2 範囲で、コミットの author / committer に
同じアドレスが入っていたことは、人が混入経路（git の設定）をたどって気づいたものだった。
メタデータを独立した検査範囲として持たせたのは公開の翌日である。

Day 2 の本文は当時の記録としてそのまま残し、訂正はここに置く。記事もこの訂正後の事実で書いている。

## 境界チェック 5 項目（この変更に対する個別実施）

検査対象コミット: README 変更コミット `c954331303a1880f53debc4f6b595a386f8496ed` と、この devlog 追加コミット（push 時点の main 全体を `scripts/boundary-check.sh` が検査する）

| # | 項目 | 結果 |
| --- | --- | --- |
| 1 | 禁止語 grep 0 件 | OK（`scripts/boundary-check.sh` が exit 0。検査語数・対象ファイル数・履歴/メタデータ込みである旨を出力で確認） |
| 2 | 公開前チェック 4 点 | OK（(1) 本文・リンクとも自作 (2) スクリーンショットなし (3) コミュニティ投稿・チャットの引用なし (4) 有料コンテンツの構成をなぞっていない） |
| 3 | 入力ソース宣言 | OK（入力は本リポジトリの公開 devlog・公開スクリプト・公開コミットと、GitHub の公開ドキュメントのみ） |
| 4 | devlog 境界 | OK（記事のタイトルと構成は候補比較で決めた。採用結果と理由のみを記録し、プロンプト・生成過程・画面は含めない） |
| 5 | スクリーンショット境界 | 該当なし（この変更で追加する画像はない） |

## 記事に書かなかったこと

- 禁止語リストの具体語・ヒットした文字列・件数の内訳（記事ではカテゴリ「個人のメールアドレス」までに留めた）
- 語リストの置き場の詳細（リポジトリ外である事実のみ）
